interface:
  display_name: "HA Config Reload"
  short_description: "Generate, check, and reload Home Assistant configuration after edits."
  default_prompt: "Use this skill after editing Home Assistant configuration files or python_templates/gen_config_yaml.py to regenerate generated YAML, run the config check, and reload the appropriate Home Assistant domains."
