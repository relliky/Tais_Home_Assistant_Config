switch:
  - platform: group
    name: name + " " + num
    entities:
      - switch.mac + "_channel_" + num


template:
  - sensor:
      - name: name + " Wall Button"
        state: states.sensor. + mac + _action.state

